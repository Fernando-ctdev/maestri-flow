export const schema=`PRAGMA foreign_keys=ON;
CREATE TABLE IF NOT EXISTS workflows(id TEXT PRIMARY KEY, source TEXT NOT NULL, hash TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS work_items(id TEXT PRIMARY KEY,parent_id TEXT REFERENCES work_items(id),title TEXT NOT NULL,body TEXT NOT NULL DEFAULT '',status TEXT NOT NULL,metadata TEXT NOT NULL DEFAULT '{}',created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL);
CREATE INDEX IF NOT EXISTS work_items_pending_top ON work_items(status,created_at) WHERE parent_id IS NULL;
CREATE TABLE IF NOT EXISTS runs(id TEXT PRIMARY KEY,workflow_id TEXT NOT NULL REFERENCES workflows(id),status TEXT NOT NULL,revision TEXT NOT NULL,created_at INTEGER NOT NULL);
CREATE TABLE IF NOT EXISTS run_actor_bindings(run_id TEXT NOT NULL REFERENCES runs(id) ON DELETE CASCADE,key TEXT, node_id TEXT NOT NULL,PRIMARY KEY(run_id,key));
CREATE TABLE IF NOT EXISTS tokens(id TEXT PRIMARY KEY,run_id TEXT NOT NULL REFERENCES runs(id) ON DELETE CASCADE,work_item_id TEXT NOT NULL REFERENCES work_items(id),flow_id TEXT NOT NULL,node_id TEXT NOT NULL,status TEXT NOT NULL,fork_stack TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS fork_instances(id TEXT PRIMARY KEY,run_id TEXT NOT NULL REFERENCES runs(id) ON DELETE CASCADE,status TEXT NOT NULL,data TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS attempts(id TEXT PRIMARY KEY,token_id TEXT NOT NULL REFERENCES tokens(id) ON DELETE CASCADE,status TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS results(id TEXT PRIMARY KEY,token_id TEXT NOT NULL REFERENCES tokens(id) ON DELETE CASCADE,outcome TEXT NOT NULL,data TEXT);
CREATE TABLE IF NOT EXISTS events(id INTEGER PRIMARY KEY AUTOINCREMENT,run_id TEXT NOT NULL REFERENCES runs(id) ON DELETE CASCADE,type TEXT NOT NULL,data TEXT NOT NULL,created_at INTEGER NOT NULL);`;
